#!/usr/bin/env bash

export ROOT_PATH="${ROOT_PATH:-<%= root %>}/warden"

if [[ -n "$@" ]]; then
  exec <%= run %> "$@"
else
  exec <%= run %> exec rake warden:start[${CONFIG_PATH:-config/linux.yml}]
fi
